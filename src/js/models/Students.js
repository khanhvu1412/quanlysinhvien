export default class Student {
    constructor(id, name, birthYear, gender, grade, avatar, course, status) {
        this.id = id;
        this.name = name;
        this.birthYear = birthYear;
        this.gender = gender;
        this.grade = grade;
        this.avatar = avatar;
        this.course = course;
        this.status = status;
    }
}